node_modules
dist
.astro
.wrangler
out
# Legacy — old Next.js build cache. Kept in gitignore so a stray local
# build from before the Astro migration doesn't accidentally get committed.
.next
.env
.env.local
.env*.local
*.log
.DS_Store
# TypeScript incremental build cache — should never be committed
*.tsbuildinfo
